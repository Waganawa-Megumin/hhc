{
  "name": "hhc",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "HHC (Human Hunter Check) — a local-first self-triage tool to assess whether a job/headhunting approach is a foreign-intelligence recruitment attempt. Outputs a risk band + recommended action; never a verdict.",
  "license": "SEE LICENSE — personal, non-commercial use",
  "workspaces": [
    "shared",
    "web",
    "agent"
  ],
  "scripts": {
    "typecheck": "tsc -p shared/tsconfig.json && tsc -p web/tsconfig.json && tsc -p agent/tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "npm run build --workspace web",
    "dev:web": "npm run dev --workspace web",
    "dev:agent": "npm run dev --workspace agent",
    "seed": "npm run seed --workspace agent"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
