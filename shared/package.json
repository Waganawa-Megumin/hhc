{
  "name": "@hhc/shared",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Single source of truth for HHC: indicators, the authoritative §3 deterministic scoring + critical-flag overrides, schemas, and guardrails. Runtime-agnostic; consumed as TS source by web (Vite) and agent (tsx).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./indicators.json": "./src/indicators.json"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
